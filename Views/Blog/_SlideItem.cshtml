@model Topic

@if ((int) ViewData["currentIndex"] == 0)
{
    <div class="carousel-item active">
        <img src="@Model.ImageURL" alt="@Model.Name">
        <div class="carousel-caption">
            <h3>@Model.Name</h3>
            <p class="short_description">Lorem ipsum dolor sit amet consectetur, adipisicing elit.</p>
        </div>
    </div>
}
else {
    <div class="carousel-item">
        <img src="@Model.ImageURL" alt="@Model.Name">
        <div class="carousel-caption">
            <h3>@Model.Name</h3>
            <p class="short_description">Lorem ipsum dolor sit amet consectetur, adipisicing elit.</p>
        </div>
    </div>
}
